from enum import Enum


class App(Enum):
    NAME = "Alejoide"
    URL = "https://alejoide.com"

class Links(Enum):
    HOME = "#"
    ABOUT = "#about"
    PROJECTS = "#projects"
    CONTACT = "#contact"